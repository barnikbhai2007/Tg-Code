# Copy this file to .env and fill in real values.
# NEVER commit the real .env file to git or share it with anyone.

# Get this from @BotFather on Telegram after running /newbot
BOT_TOKEN=8409792841:AAGk33yimiCHRVDlyoVYBpfP1y2d0e1u2TU

# Your personal numeric Telegram user ID (get it from @userinfobot)
ADMIN_CHAT_ID=8238949394

# A long random secret used by trusted approvers to approve payments
# via /approve command instead of tapping the inline button.
# Generate one with:
#   python3 -c "import secrets; print(secrets.token_urlsafe(24))"
ADMIN_APPROVAL_TOKEN=Nuiw_LGmXVC2bdCHGkJKfKxYarRp3eXH

# Gmail address that receives your site's verification codes
EMAIL_ADDRESS=webblogger82@gmail.com

# Google App Password (NOT your Gmail login password).
# Requires 2FA enabled on your Google account.
# Generate at: https://myaccount.google.com/apppasswords
EMAIL_APP_PASSWORD=opnk zlsp znmk dsjb

# Each site that sends you sign-in codes. Add as many as you need by
# following the same CODE_SOURCE_N_NAME / CODE_SOURCE_N_SENDER pattern
# (N = 3, 4, ...). NAME is what users see on the button; SENDER must be
# the exact address that site sends codes FROM.
CODE_SOURCE_1_NAME=NETFLIX
CODE_SOURCE_1_SENDER=info@account.netflix.com

CODE_SOURCE_2_NAME=PRIME VIDEO
CODE_SOURCE_2_SENDER=noreply@yoursite2.com

# How long a subscription lasts after approval
SUBSCRIPTION_DAYS=30
SUBSCRIPTION_PRICE_INR=150

# Your UPI ID shown to users for payment
UPI_ID=brokenbhowmikaqua@fam

DB_PATH=subscriptions.db
